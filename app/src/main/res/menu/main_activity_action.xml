<menu xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_choose_file"
        android:icon="@drawable/choose_file"
        android:title="ok"
        android:showAsAction="always" />
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_choose_chapter"
        android:title="np"
        android:icon="@drawable/choose_chapter"

        android:showAsAction="always" />
</menu>